# pir-service-protocol

Kept BitcoinPIR service identifiers and operation-start wire types. This crate
contains no network, filesystem, Lightning, Cashu, ARC, or admission logic.

The previous signed-policy / clearing / directory surface lived here and has
been deleted. Dated records are in `docs/archive/payment/`.
