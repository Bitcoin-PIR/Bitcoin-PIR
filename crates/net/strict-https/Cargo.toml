[package]
name = "pir-strict-https"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
httparse = "1"
rustls = { version = "0.23", default-features = false, features = ["ring", "std", "tls12"] }
webpki-roots = "0.26"
zeroize = "1"
