# Historical Planning Notes

This directory contains older implementation plans that are still referenced
from source comments, verification docs, or operator notes.

Root-level `PLAN_*.md` symlinks are kept as compatibility shims for existing
references. New durable design plans should live under `docs/` or
`docs/plans/`; private scratch notes should use `LOCAL_PLAN_*.md`, which is
ignored by git.
